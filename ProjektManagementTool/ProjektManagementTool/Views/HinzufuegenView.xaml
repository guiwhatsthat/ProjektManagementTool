<Window x:Class="ProjektManagementTool.Views.HinzufuegenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektManagementTool.Views"
        mc:Ignorable="d"
        Title="Hinzufügen" Height="250" Width="328">
    <Grid>
        <StackPanel Margin="0,13,0,43" HorizontalAlignment="Center">
            <GroupBox Header="{Binding Header}" Height="178" Width="275">
                <StackPanel>
                    <DataGrid ItemsSource="{Binding ListObj}" SelectedIndex="{Binding Index}" Height="117">

                    </DataGrid>
                    <Button Command="{Binding CMDZuweisen}">Zuweisen</Button>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</Window>
